# props 检测

```javascript
    export default {
        props:{
            // 开启对num的检测
            num: {
                type:类型,
                default:默认值，
                required:布尔值
            }
        }
    }
   
```